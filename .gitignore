.env
.env.bak
.env.backup
# Track node_modules so the package works after a GitHub sync,
# but exclude the plivo test/secret file.
node_modules/plivo/dist/rest/request-test.js
tmp/
.gitignore
# Editor/generated file backups (not source)
_file_backups/
backend/_file_backups/

# Generated backup artifacts — never commit these
_backups/
*.bak
*.backup
*.old
*~
