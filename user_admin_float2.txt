-8<---
{
  "target": "frontend/auth.js",
  "purpose": "Remove the 'Float Number' input from the top of the Float & Riders panel (float assignment is owned by the float admin tool)."
}
```old
        <div class="form-group" style="margin-bottom:0.75rem;">
          <label style="font-size:0.8rem;color:#b8c4e0;display:block;margin-bottom:0.3rem;">Float Number</label>
          <input id="uem-float-number" type="text" value="${escHtml(full.member_float_number||'')}" placeholder="e.g. 12" maxlength="20" style="width:100%;padding:0.65rem 0.9rem;border-radius:10px;border:1px solid rgba(255,255,255,0.12);background:rgba(255,255,255,0.04);color:#f5f7ff;font:inherit;box-sizing:border-box;" />
        </div>
```new

-8<---
{
  "target": "frontend/auth.js",
  "purpose": "Check the Float Captain box when the float admin has this user as captain (captain_of); show 'Captain of Float #N - Name' inline beside 'Float Captain' in white."
}
```old
        <div class="form-group" style="margin-bottom:0.75rem;">
          <label style="display:flex;align-items:center;gap:0.6rem;cursor:pointer;font-size:0.9rem;font-weight:600;padding:0.7rem 1rem;border-radius:10px;border:1px solid rgba(255,210,98,0.2);background:rgba(255,210,98,0.06);">
            <input type="checkbox" id="uem-float-captain" ${full.float_captain ? 'checked' : ''} style="width:1.1rem;height:1.1rem;cursor:pointer;accent-color:#ffd262;" />
            Float Captain
          </label>
          ${full.captain_of ? `<div style="margin-top:0.5rem;font-size:0.85rem;color:#ffd262;">Captain of Float #${escHtml(full.captain_of.float_number || '?')} – ${escHtml(full.captain_of.name || 'Unnamed')}</div>` : ''}
        </div>
```new
        <div class="form-group" style="margin-bottom:0.75rem;">
          <label style="display:flex;align-items:center;gap:0.6rem;cursor:pointer;font-size:0.9rem;font-weight:600;padding:0.7rem 1rem;border-radius:10px;border:1px solid rgba(255,210,98,0.2);background:rgba(255,210,98,0.06);">
            <input type="checkbox" id="uem-float-captain" ${full.float_captain || full.captain_of ? 'checked' : ''} style="width:1.1rem;height:1.1rem;cursor:pointer;accent-color:#ffd262;" />
            Float Captain
            ${full.captain_of ? `<span style="margin-left:0.7rem;color:#ffffff;font-weight:500;">Captain of Float #${escHtml(full.captain_of.float_number || '?')} – ${escHtml(full.captain_of.name || 'Unnamed')}</span>` : ''}
          </label>
        </div>
-8<---
{
  "target": "frontend/auth.js",
  "purpose": "Stop the save from blanking member_float_number now that the input is gone; preserve the loaded value so the member stays linked to their float."
}
```old
      member_float_number: backdrop.querySelector('#uem-float-number')?.value.trim() || '',
```new
      member_float_number: full.member_float_number || '',
